# SENTINEL currently runs without required environment variables.
# Keep this file as the template if future runtime/config secrets are added.
#
# Example:
# API_BASE_URL=
# OPTIONAL_FEATURE_FLAG=false
